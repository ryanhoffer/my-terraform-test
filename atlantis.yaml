version: 3
automerge: false
projects:
- name: my-terraform-project
  dir: .
  autoplan:
    enabled: true
    when_modified: ["*.tf", "*.tfvars"]
  workflow: default

workflows:
  default:
    plan:
      steps:
      - init
      - plan
    apply:
      steps:
      - apply